# ── Book Build Only (GitHub Actions CI) ────────────────────
# Classic Jupyter Book 0.15.x — Sphinx 4/5 based.
# Stay <1.0.0 — jupyter-book 1.x is the myst-cli engine (different config format).
# Do NOT override sphinx-design here — JB 0.15.1 pins ~=0.3.0 internally.
jupyter-book==0.15.1

# Extra Sphinx extensions (compatible with Sphinx 5.x)
sphinx-togglebutton>=0.3.2
sphinx-copybutton>=0.5.2
jupyterlite-sphinx>=0.9.3,<0.13
